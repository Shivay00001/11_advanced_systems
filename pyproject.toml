[project]
name = "advanced-systems"
version = "1.0.0"
description = "Resilience patterns, rule evaluation, and fault-tolerant system foundations"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "VisionQuantech Custom Commercial License"}

dependencies = [
    "pydantic>=2.5.0",
    "tenacity>=8.2.0",
    "structlog>=24.1.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
